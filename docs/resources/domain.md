---
page_title: "qrator_domain Resource - terraform-provider-qrator"
subcategory: ""
description: |-
  Manages a domain in Qrator.
---

# qrator_domain (Resource)

Manages a domain in Qrator — its name and access policy.

~> **Note:** Deleting this resource is a no-op — the domain itself is not removed.

Use `qrator_domain_services` to manage the service list, `qrator_domain_sni` for SNI configuration, and `qrator_domain_whitelist` / `qrator_domain_blacklist` for IP lists.

## Example Usage

```terraform
resource "qrator_domain" "example" {
  id   = 12345
  name = "example.com"
}
```

### Restrict access to whitelisted IPs only

```terraform
resource "qrator_domain" "restricted" {
  id                      = 12345
  name                    = "internal.example.com"
  not_whitelisted_policy  = "drop"
}

resource "qrator_domain_whitelist" "allowed" {
  domain_id = 12345

  entries = [
    {
      ip      = "203.0.113.10"
      comment = "Office gateway"
    }
  ]
}
```

## Import

Existing domains can be imported using the domain ID:

```shell
terraform import qrator_domain.example 12345
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (Number) The domain ID. Changing this forces resource replacement.

### Optional

- `name` (String) The domain name.
- `not_whitelisted_policy` (String) Access policy for non-whitelisted IPs: `accept` or `drop`. Defaults to `accept`.
